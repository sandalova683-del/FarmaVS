package com.formulavs.health

// Register the plugin from the app's Capacitor MainActivity / bridge setup.
// Example for a Capacitor app that exposes a custom plugin list:
// registerPlugin(FormulaVSHealthPlugin::class.java)
